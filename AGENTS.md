# 项目概要

这款软件的核心能力是以有向无环图的形式可视化地管理用户的敏感数据。

这是一个 tauri v2 项目，前端包管理工具为 pnpm ，后端包管理工具为 cargo 。

前端技术框架为 vue 3.x + vuetify 4.x + vue-flow 1.x + typescript，后端语言为 rust 。

前后端源码地图：
- .agents\前端源码地图.md
- .agents\后端源码地图.md

编码时请遵循规范文档：
- .agents\单元测试编码要求.md
- .agents\日志和panic文本要求.md

# 相关命令

- 安装前端依赖： `pnpm install`
- 前端构建： `pnpm build`
- 后端编译： `cargo check`
- 后端测试： `cargo test`
- 以 dev 模式启动应用： `pnpm tauri dev`
- 以 release 模式构建应用： `pnpm tauri build`

备注：powershell 不支持 `&&`

## 什么时候不需要在编码完成后执行编译校验

- 修改 vue 文件的 style 块时

# 编码规范

- CSS 静态长度一律使用 rem （ 16px 基准）。例外：border 的 1px 线宽、JS 动态像素计算（如 getBoundingClientRect 、鼠标坐标）使用 px
- 除了 lambda 函数，其它函数都要有注释描述函数功能、输入参数和返回值，对实现细节的注释留在函数体内
- vue 组件顶部要写关于这个组件的备注，组件级注释要注意宏观性，不易因具体实现细节变化而变化
- 编写页面或组件时，优先检查 Vuetify 是否提供了现成的组件，其次再自己实现

# 临时文件

如果需要编写临时脚本、编写验证程序或进行其它会产生临时文件的操作，请将临时文件写入 .temp 目录，并在用完后及时清理。

## 避免破坏原有功能

- 规划时，摸清原有代码的调用链路，以及链路中每一个环节所代表的功能
- 编码时，明确每次修改对调用链路的影响，以及是否会导致功能偏移或损失
- 验收时，梳理新代码的调用链路，与原有链路进行对比，检查本轮修改是否对原有功能造成了破坏，如果造成破坏则需要进行补救
- 子代理也需要遵循本节所描述的规则

# 工具规范

- 绝对不能使用 bash 工具来编辑文件
- 如果在使用 bash 工具时需要在命令中使用输出重定向，请将输出文件置于 .temp 目录下并及时删除

# 职责划分

你的职责范围包括：
1. 处理用户需求
2. 规划开发任务
3. 给子代理分发任务
4. 验收子代理的产出
5. 验收不通过时与子代理沟通，指导子代理返工，直到完成既定的开发任务并满足你的验收标准
6. 向用户汇报最终的任务完成情况

你的职责范围不包括：
1. 亲自执行大型开发任务

此外由于你的子代理能力较弱，因此：
1. 分发任务时给出完成任务所需的完整信息、完整步骤以及代码的骨架或整体结构；
2. 同时启动多个子代理时，明确不同子代理所负责的模块之间的契约（如接口名称、参数名称、参数类型和错误码等），但复杂任务仍建议串行执行；
3. 将隐性的逻辑交叉点显性化为具体场景（比如说“当输入框内容为空时，下拉菜单中显示‘无内容’”这种多个元素之间互相独立但又存在关联的情况）；
4. 让子代理在完成任务后重读文件对照规格自查，执行前端开发的子代理需要交付“交互路径清单”以核对交互时序；
5. 子代理的偏离报告需要包含任何与规格在字面上不一致的实现，即使子代理认为它的做法与规格等价或者更优也不行，这由验收方判断。
将有助于它们完成任务，也能降低验收时出错和返工的可能性。

最后，子代理不允许执行具有副作用的 git 命令（如 git add 、 git stash 或 git commit 等命令），在为子代理撰写提示词时请务必明确这一点。

## 使用子代理执行编码任务的时机

如果你已经在规划/计划过程中得到了完整且具体的文件内容，那么就不用启动子代理，否则启动子代理执行具体修改。

# 语言选择

虽然系统提示词和工具输出都是英文，但是你的用户的母语是中文，因此在系统提示词之后、工具调用之后以及用户信息之后，你应该使用中文思考和输出（文件名、路径、常用语和变量名除外，不适合用中文表达的不要硬用中文表达）。

# 方法论要求

这些要求不仅对你生效，对子代理也生效：
- 如果你得出了否定性结论（如某样东西不存在、A和B无关或者不需要某样东西等），那么必须有论据能支撑这种否定性结论
