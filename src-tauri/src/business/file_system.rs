//! 只读文件系统查询业务模块，供前端自研文件选择器调用。
//!
//! 本模块只提供读取能力（列目录、根路径列表、路径存在性判断），
//! 不提供任何写入或删除接口。
//!
//! 模块划分：
//! - [`service`]：文件系统查询的业务流程（主目录、列目录、根路径列表）。
//! - [`vo`]：面向前端的值对象定义。
//! - [`command`]：面向前端的 `#[tauri::command]` 入口。

pub mod command;
pub mod service;
pub mod vo;
