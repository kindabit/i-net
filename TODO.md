# 高

[ ] 在标题栏显示版本号
[ ] Home.vue 页面确认密码的地方 tab 键顺序错误
[ ] 节点的标题和副标题改成一行
[ ] 二次加密应该使用不同的密码（？可能没有意义）
[ ] 选中节点时高亮与之相关的边
[ ] 点击画布空白处关闭回收站，就像新建节点菜单
[ ] 放开画布间的连接，允许画布存在影子节点，但只在源画布中创建目标画布的影子节点，允许普通节点和画布的影子节点相连，在画布的影子节点所指的画布中创建影子节点
- 普通节点 -> 普通节点：和影子节点无关
- 普通节点 -> 画布节点：在画布节点创建入向影子节点
- 画布节点 -> 普通节点：在画布节点创建出向影子节点
- 画布节点A -> 画布节点B：在A创建B的出向影子节点（新）
- 普通节点 -> 普通节点的影子节点：无级联作用
- 普通节点 -> 画布节点的影子节点：在目标画布节点中创建影子节点
- 画布节点 -> 

依赖关系的两个方面：
- 谁在依赖我
- 我在依赖谁
对于存在跨画布依赖关系的节点，如果我把源画布和

影子节点的设计目标：
1. 子画布中的普通节点可能需要依赖父画布中的普通节点，因此父画布中的普通节点可以在子画布中建立影子节点供子画布中的普通节点依赖，操作方式是以父画布中的普通节点为起点建立一条指向子画布画布节点的边
2. 平级的两个画布A和B，画布B中的普通节点可能需要依赖画布A中的普通节点，因此允许画布B在A中创建影子节点，随后画布A中的普通节点可以借由画布B的影子节点在画布B中创建影子节点供画布B中的普通节点依赖

影子节点是因为边存在而存在的。

shadow_id 应该指向边，而不是节点，这样即使存在多重级联关系，只要删除最初的那一条边，整个级联体系都能自然地被整体摧毁。

|节点相连作用关系|普通节点（目标）|画布节点（目标）|普通节点的影子节点（目标）| 画布节点的影子节点（目标）|
|-|-|-|-|-|
|普通节点（源）|无级联影响（含义为“目标依赖源”）|在目标画布创建源普通节点的影子节点（含义为“目标画布中的节点可能需要依赖该源普通节点”）|非法连接（普通节点的影子节点现在只能是入向影子节点，无法作为目标）|向上查找目标影子节点的本体画布，在本体画布创建源普通节点的影子节点（含义为“目标节点的本体画布中的节点可能需要依赖该源普通节点”）|
|画布节点（源）|非法连接（见备注1）|在源画布节点创建目标画布节点的影子节点（含义为“目标画布中的节点可能需要依赖源画布中的节点，因此需要在源画布中创建目标画布的影子节点，以便所需依赖项进一步在目标画布中建立影子节点”）|非法连接（普通节点的影子节点现在只能是入向影子节点，无法作为目标）|向上查找目标影子节点的本体画布，在源画布中创建指向目标本体画布的影子节点（含义类似“画布节点（源） -> 画布节点（目标）”，但是多一层“目标画布中的节点可能需要依赖源当前画布的子画布中的节点”的含义）|
|普通节点的影子节点（源）|无级联影响（含义为“目标依赖源影子节点的本体”）|在目标画布节点创建影子节点的影子节点（含义为“目标画布中的节点可能需要依赖源影子节点所指的普通节点”）|非法连接（普通节点的影子节点现在只能是入向影子节点，无法作为目标）|非法连接（目标的本体或者上级影子节点直接或间接地存在于父画布中，源的本体或者上级影子节点也直接或间接地存在于父画布中，如果两者需要连接，应该直接在父画布中连接，而不是先接入子画布再连接）|
|画布节点的影子节点（源）|非法连接（画布节点的影子节点现在只能是出向影子节点，无法作为源）|非法连接（画布节点的影子节点现在只能是出向影子节点，无法作为源）|非法连接中的非法连接|非法连接（画布节点的影子节点现在只能是出向影子节点，无法作为源）|

备注1：这意味着目标普通节点需要依赖源画布中的节点。我们不允许在源画布中建立目标普通节点的影子节点，因为这可能会导致目标普通节点的依赖项散落在各个不同的画布中，而我们希望依赖项能够集中显示在目标节点的周围，从而避免用户需要一个画布一个画布地查看依赖情况。如果遇到这种需要依赖子画布中的节点的情况，应该先新建一个子画布，然后将目标普通节点放进新的子画布中，再将源画布节点和新的子画布节点相连，这样源画布中就会出现新的子画布的影子节点，然后再在源画布中将目标普通节点需要依赖的源普通节点与新的子画布的影子节点相连，这样新的子画布中就会出现所需源普通节点的影子节点，最后再将源普通节点的影子节点与依赖它的目标节点相连。

[ ] 是不是应该给影子节点单独新建一个 service ？现在有一些在 node 里，有一些在 edge 里。
[ ] src-tauri\src\business\user_database\node\service\list.rs 递归查询可以用 prepared statement 优化
[ ] 问问 ai 对我的代码地图的看法？如何才能让 ai 也易于理解这个项目？
[ ] 教程引导系统（等自动化e2e测试）
[ ] 视觉自动化流程
- [x] 不要把屏幕坐标传给大模型，只传图像内的坐标，实际操作鼠标时再转换为全局坐标
- [ ] 找一个速度比较快的具有图像理解能力的价格较低的模型，最好在关闭思考模式的情况下也有较高的质量（关闭思考模式不现实）
- [x] Omniparser V2 屏幕图像解析（ WSL AMD GPU 加速）
- [ ] 寻找屏幕录制软件
- [ ] 桌面自动化（ pyautogui ）
- [ ] 端到端测试流程 + 教学视频流程编排（脚本驱动）
- [ ] 桌面端端到端测试

搭建 WSL2 环境，在 WSL2 环境中部署 omniparser v2
使用 python 接入 WSL2 中的 omniparser v2
使用 python 接入 kimi
准备 ffmpeg
使用 pytest 编写测试脚本和录制脚本

# 中

[ ] AGENTS.md 中应该写出拆分任务给子代理的案例
[ ] 清空 → 移动之间无回滚
[ ] 发布介绍页和看板页
[ ] 桌面端发布
[ ] 自动化视频教程录制方案

# 低

顺序：测试 -> 云端同步 -> 安卓端 -> 其它操作系统

[ ] 前端针对 command 层的测试
[ ] 云端同步
[ ] 安卓端
[ ] 支持 linux 操作系统
[ ] 打开选择文件和目录的对话框时，默认的目录

# 极低

[ ] 自动布局未能正常触发自动吸附（暂时无法复现）
[ ] 创建完组合后，就无法再进行 lasso-select 了（暂时无法复现）
[ ] 字段类型名称国际化（不是即时国际化，刷新页面可以解决问题）
[ ] Word 表格宽度异常地小（腾讯文档的问题）
[ ] AntV G6 自带的上下文菜单（ AntV 自带的这个上下文菜单好像有点问题）
[ ] SikuliX 使用自动化脚本录制教程视频（在使用 Omniparser 的情况下，可能已经不需要了）

# 废弃

[ ] 自己打包 x2t （已经采用其它的文件预览方案）
[ ] 加快启动速度（ release 包的启动速度尚可）
[ ] 优化中国大陆和美国的与定义全局模板库（预定义模板库的功能已经被砍掉了）
[ ] k3 太慢了，改成 k2.6 和 k2.7 试试（2.6也很慢）
[ ] 双击画布节点切换画布不会持久化当前画布的视口，但是点击影子节点的虚拟边切换到父画布却会持久化子画布的视口（这不是 bug ）

# 已完成

[x] 把 DataNodeData 里的 canvasId 重命名为 canvasRefId
[x] 同步画布节点标题和画布宇宙中的画布节点的标题
[x] 不允许影子节点之间互相连接
[x] 在两个已经相连的节点之间重新建立新的边会直接替换旧的边
[x] 节点的宽高常量
[x] 将节点移入子画布或父画布
[x] 批量选中节点，批量移动（左键选择，右键拖拽移动）
[x] 复制节点
[x] password 输入框隐藏按钮重复问题
[x] 备份进度条是没有用的
[x] 面包屑可以再稍微长一些
[x] typeof 用 lodash 替换
